Vendored PDF font assets for the sheet generator and annotator.

Selection order:
- Latin Modern first
- Computer Modern fallback

Files sourced from CTAN packages:
- `fonts/lm` for `lmr10` and `lmbx10`
- `fonts/amsfonts` for `cmr10` and `cmbx10`
